BootStrap: library
From: ubuntu:20.04

%files
    ~/PPI_Prediction_byCoevolution/containers/conda_envs/py37_notebook_environment.yml  /home/conda_envs/py37_notebook_environment.yml

%post
    apt-get update && apt-get -y upgrade
    apt-get -y install \
    build-essential \
    wget \
    less 

    wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh 
    bash  Miniconda3-latest-Linux-x86_64.sh -b -u -p /home/miniconda3 
    
    . /home/miniconda3/etc/profile.d/conda.sh
    

    # this is necessary for nextlfow, so the need envs is activated without extra operation
    echo ". /home/miniconda3/etc/profile.d/conda.sh" >> $SINGULARITY_ENVIRONMENT
    echo "conda activate py37_notebook" >> $SINGULARITY_ENVIRONMENT
    
    
    /home/miniconda3/bin/conda env  create  -f /home/conda_envs/py37_notebook_environment.yml
    
    conda activate py37_notebook  
    
    pip install --upgrade jupyter_client  # find this necessery when test in virtural machine , and packate networkx ?
    # ImportError: Can't determine version for numexpr, pip install numexpr
    
    # seem idea, install py37_scikit learn and then install py3d mol from it ?
           
    conda info --envs
    
    
    

%environment
    export LC_ALL=C
    export PATH=$PATH:/home/miniconda3/bin/conda # check this:   /root/.bashrc, this is nomal linux exprot grammer "export PATH="/home/miniconda3/bin/conda:$PATH""
    
    

%runscript
    echo "this section defines actions for the container to take when it is executed. (These commands will therefore not be run at build time.)"
    


# cd ~/PPI_Prediction_byCoevolution/containers
# or  singularity build --fakeroot ~/singularity_containers/py37_notebook.sif ~/PPI_Prediction_byCoevolution/containers/py37_notebook.def
# or singularity build --remote ~/singularity_containers/remoteBuild/py37_notebook.sif ~/PPI_Prediction_byCoevolution/containers/py37_notebook.def


# or create two conda enviroment in this containers , and install ipykernel to allow seperated selection
#Set notebook conda and container
# conda create -n py37_notebook python=3.8
# conda activate py37_notebook
# conda install ipykernel
# python -m ipykernel install --user --name py37_notebook --display-name "py37_notebook"
# conda install -c conda-forge matplotlib=3.5.1 # or 3.5.3?
# conda install -c anaconda seaborn=0.12.2
# conda install -c conda-forge py3dmol=1.8.1  
# conda install -c anaconda scikit-learn=1.0.2 
# conda install -c conda-forge biopython=1.80
